{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "lint": {},
    "types": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", "plugin/build/**"]
    }
  }
}
