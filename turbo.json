{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "dev": {
      "dependsOn": ["types"],
      "cache": false
    },
    "lint": {},
    "types": {},
    "build": {
      "dependsOn": ["^build", "types"],
      "outputs": ["dist/**", "build/**", "plugin/build/**"]
    }
  }
}
